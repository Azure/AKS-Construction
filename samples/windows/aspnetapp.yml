apiVersion: apps/v1
kind: Deployment
metadata:
  name: samplewindowsapp
  labels:
    app: samplewindowsapp
spec:
  replicas: 1
  template:
    metadata:
      name: samplewindowsapp
      labels:
        app: samplewindowsapp
    spec:
      nodeSelector:
        "kubernetes.io/os": windows
      containers:
      - name: sample
        image: mcr.microsoft.com/dotnet/framework/samples:aspnetapp
        resources:
          limits:
            cpu: 1
            memory: 800M
        ports:
          - containerPort: 80
  selector:
    matchLabels:
      app: samplewindowsapp
---
apiVersion: v1
kind: Service
metadata:
  name: samplewindowsapp
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
  selector:
    app: samplewindowsapp
